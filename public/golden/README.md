# Golden WaveDrom fixtures

Copied from `public/samples/` for round-trip regression tests (`src/wavedromBridge/goldenRoundTrip.test.ts`). Edit samples first, then refresh golden copies if intentional.
